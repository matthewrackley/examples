# Use the Debian-based PostgreSQL image
FROM postgres:11.12

# Set environment variables for PostgreSQL
ENV POSTGRES_USER=database_user
ENV POSTGRES_PASSWORD=database_password
ENV POSTGRES_DB=todo_db
